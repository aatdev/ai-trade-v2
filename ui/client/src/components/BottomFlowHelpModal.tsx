import Markdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import { Modal } from './ui';

const HELP = `
## Как работать со скринером «дно + дивергенция потока»

Под-вкладка прогоняет \`bottom-flow-divergence-screener\` — ищет бумаги, у которых
**цена на дне** (близко к 52-недельному минимуму и глубоко ниже 52-недельного хая),
но **поток не подтверждает это дно**. Два слоя дивергенции:

- **Фундамент** — выручка (TTM) ещё растёт *и* операционный денежный поток > 0
  (бизнес не сломался — паттерн HOOD).
- **Накопление** — Chaikin Money Flow > 0 *или* Money Flow Index ≥ порога
  («умные деньги» набирают на дне — контрарианский паттерн «MRNA-типа»).

**Detection-only.** Скрин ничего не записывает и не выставляет заявок. Источник
данных — публичный \`scanner.tradingview.com\` (тот же эндпоинт, что и вкладка
«All stocks» в TradingView): **API-ключ и TradingView Desktop не нужны**.
Промежуточный файл лежит в \`trading-data/ui-staging/\` и не виден остальному дашборду.

> ⚠️ «Дно» — это **кандидат**, а не подтверждённый разворот. Скрин находит бумаги
> у минимума, но не гарантирует, что минимум поставлен. Используй гейт «только
> разворот» и подтверждай базу на графике, чтобы не ловить падающий нож.

### Порядок действий

1. **▶ Запустить скрин** — гоняет скринер с вашими параметрами. Кандидаты
   сгруппированы по грейду и отсортированы по composite score.
2. Разверните строку (▸), чтобы увидеть денежные потоки, маржу, выживаемость и
   предупреждение о возможном M&A. Клик по тикеру открывает график.

### Грейды

| Грейд | Условие | Чтение |
|---|---|---|
| **A** | дно + фундамент **И** накопление | потоки здоровы И тейп набирает — лучший сетап |
| **B-accum** | дно + только накопление | тейп набирает при слабом/отрицательном фундаменте (контрарианский, рисковее) |
| **B-fund** | дно + только фундамент | бизнес в порядке, но тейп ещё не развернулся |

### Гейты и как они соединяются (И / ИЛИ)

**Гейт** — это условие-фильтр: бумага должна его пройти, чтобы попасть в список.
Одни условия требуют выполнения **всех сразу** (связка **И**), другим достаточно
**любого одного** (связка **ИЛИ**). Здесь это устроено так:

**1. Гейт «дно» — обязательный, связка И.** Бумага проходит, только если выполнено
*и то, и другое*: цена близко к 52-недельному минимуму (≤ «% над дном») **И** цена
глубоко ниже 52-недельного хая (≥ «% ниже хая»). Зачем оба: «близко к минимуму» без
глубокого падения — это узкий боковик у лоу, а не настоящее дно. Не прошёл хотя бы
одно — бумага отсеивается сразу.

**2. Два слоя дивергенции — определяют грейд.**
- **Фундамент** = рост выручки TTM выше порога **И** операционный денежный поток > 0
  (связка **И** — нужны оба признака живого бизнеса).
- **Накопление** = Chaikin Money Flow > 0 **ИЛИ** Money Flow Index ≥ порога
  (связка **ИЛИ** — достаточно любого индикатора притока денег).

Грейд — это комбинация двух слоёв: **A** = фундамент **И** накопление;
**B-accum** = накопление, но **НЕ** фундамент; **B-fund** = фундамент, но **НЕ**
накопление. Бумага, не прошедшая ни один слой, в список не попадает.

**3. Выживаемость — связка ИЛИ.** Бумага считается «выживаемой», если выполнено
*хотя бы одно*: чистая прибыль > 0 **ИЛИ** FCF > 0 **ИЛИ** Altman Z > 3 **ИЛИ**
current ratio > 1.5. ИЛИ здесь намеренно мягкое: достаточно одного признака здоровья
баланса. Altman Z ненадёжен для банков и REIT — поэтому ИЛИ, а не И, чтобы не
выбрасывать здоровые финансовые компании.

**4. Необязательные галочки-гейты — добавляются поверх через И.**
- **Только разворот (turn)** — оставить лишь бумаги, которые уже разворачиваются:
  perf за 3 мес ≥ 0 **ИЛИ** цена выше SMA50 (само «разворот» — связка ИЛИ).
- **Только выживаемые** — выбросить тех, кто не прошёл гейт выживаемости (п. 3).

Включишь обе — они работают вместе (связка **И**): бумага должна одновременно
подходить по грейду, **И** быть в развороте, **И** быть выживаемой. По умолчанию
обе выключены, чтобы не срезать контрарианский слой B-accum (он часто убыточный).

**5. Выбор грейдов (галочки A / B-accum / B-fund) — связка ИЛИ.** Бумага остаётся,
если её грейд совпал с *любым* отмеченным (между галочками — ИЛИ). Нужен ≥ 1 грейд.

**Шпаргалка — что через И, что через ИЛИ:**

| Условие | Связка | Почему |
|---|---|---|
| дно (близко к лоу + глубоко от хая) | **И** | нужно и близко к минимуму, и сильное падение |
| фундамент (выручка + опер. поток) | **И** | оба признака живого бизнеса |
| накопление (CMF / MFI) | **ИЛИ** | хватает любого сигнала притока денег |
| грейд A | **И** | фундамент И накопление одновременно |
| выживаемость (прибыль / FCF / Altman / current) | **ИЛИ** | хватает одного признака устойчивости |
| разворот (perf 3м / выше SMA50) | **ИЛИ** | любой признак, что падение остановилось |
| галочки-гейты + выбор грейда | **И** между группами | все включённые ограничения применяются вместе |
| несколько отмеченных грейдов | **ИЛИ** | подходит любой отмеченный грейд |

### Параметры

Поля предзаполнены дефолтами скрипта. Пустое числовое поле = дефолт.

| Параметр | Что значит | Дефолт |
|---|---|---|
| **Вселенная** | обыкновенные акции / + привилегированные | common |
| **Грейды** | какие грейды оставить (нужен ≥1) | A + B-accum + B-fund |
| **Только разворот** | оставить лишь Perf.3M ≥ 0 или цена > SMA50 | выкл |
| **Только выживаемые** | отсечь убыточные со слабым балансом | выкл |
| **≤ % над 52н дном** | насколько близко к минимуму («на дне») | 25 |
| **≥ % ниже 52н хая** | глубина просадки от хая («сильно упал») | 35 |
| **Min рост выручки TTM** | порог фундаментального слоя | 0 |
| **Min MFI** | порог накопления (Money Flow Index) | 50 |
| **Max perf за год** | пред-фильтр: насколько бумага должна быть просевшей | −10 |
| **Min кап. / оборот / цена** | фильтры ликвидности | $1B / 500K / $5 |
| **Топ в отчёте** | сколько строк показать (0 = все) | 25 |

### Теги

- **▲ разворот / ▽ падает** — Perf.3M ≥ 0 или цена выше SMA50 (формируется база vs
  ещё летит вниз).
- **восстанавливается** (QoQ-выручка ускоряется) vs **устойчивый** (TTM-рост высок и
  просто не падал) — ось «восстанавливаются» против «не падали вовсе».
- **⚠ M&A?** — рост подозрительно высок (вероятно поглощение, не органика) — проверь
  вручную.
- **флаги риска** — убыток / FCF<0 / низкий Altman Z. *Altman Z ненадёжен для
  финансовых компаний и REIT* — там это обычно шум (выживаемость считается по ИЛИ:
  net income, FCF, Altman Z или current ratio).

### Композитный score

Взвешенная сумма силы потока (рост выручки и его ускорение QoQ, FCF-маржа),
накопления (CMF / MFI выше нейтрали) и замедления падения (Perf.3M − Perf.6M),
плюс бонусы за выживаемость и разворот. Score сравнивает бумаги **внутри** грейда.

### Совет по интерпретации

A-грейд часто кучкуется в одном секторе (например, софт/финтех при переоценке вниз):
они упали по одной макро-причине и, скорее всего, развернутся вместе — относись к
кластеру как к одной ставке, а не к N независимым.
`;

export default function BottomFlowHelpModal({ onClose }: { onClose: () => void }) {
  return (
    <Modal
      title="❓ Документация — скринер «дно + дивергенция»"
      onClose={onClose}
      wide
      footer={<button onClick={onClose}>Закрыть</button>}
    >
      <div className="md">
        <Markdown remarkPlugins={[remarkGfm]}>{HELP}</Markdown>
      </div>
    </Modal>
  );
}
