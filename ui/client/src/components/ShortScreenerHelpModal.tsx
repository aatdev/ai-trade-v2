import Markdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import { Modal } from './ui';

const HELP = `
## Как работать со скринером шортов

Под-вкладка прогоняет \`swing-short-screener\` — зеркало VCP для короткой стороны:
ищет бумаги в **стадии 4 (Stage 4, нисходящий тренд)**, слабее рынка, и отдаёт
размеченный watchlist. **Detection-only**: скрин ничего не записывает и не
выставляет заявок. Промежуточный файл лежит в \`trading-data/ui-staging/\` и не
виден остальному дашборду.

> ⚠️ Шорты — инструмент для **слабого рынка**. Перед входом обязательно подтвердите
> у брокера наличие бумаги для шорта (borrow/locate) и статус SSR (Rule 201,
> аптик-правило после падения ≥10% за день).

### Порядок действий

1. **▶ Запустить скрин.** Гоняет скринер с вашими параметрами по живым данным
   TradingView. Результат — кандидаты, отсортированные по composite score
   (выше score = более подтверждённая слабость).
2. Разверните строку кандидата (▸), чтобы увидеть вклад каждого из пяти факторов
   и ключевые метрики. Клик по тикеру открывает график с уровнями
   вход / стоп / цель.

Шага «Построить план» и «Сохранить watchlist», как у лонгов, здесь нет: у короткой
стороны нет планировщика-аналога, а кандидат уже несёт готовые уровни сделки.

### Параметры

Поля предзаполнены дефолтами самого скрипта. Пустое поле = дефолт. \`Max
candidates\` пусто ⇒ сканируется **вся S&P 500** (\`--full-sp500\`); число —
ограничить сверху.

| Параметр | Что значит | Дефолт |
|---|---|---|
| **Вселенная** | S&P 500 / широкая NASDAQ+NYSE (файл) / свой список | wide, если есть файл |
| **мин. грейд** | какие грейды оставить в отчёте (A/B/C/D) | C |
| **топ в отчёте** | сколько строк показать (0 = все) | 25 |
| **RS lookback** | за сколько сессий считать слабость против индекса | 63 |
| **min цена $** | отсекает дешёвые бумаги (риск сквиза low-float) | 5 |
| **min оборот $M** | мин. средний дневной оборот в $ млн (ликвидность/borrow) | 3 |
| **min стоп %** | отсечь стопы тоньше N% (шум — гарантированный вынос) | 2 |
| **max стоп %** | отсечь стопы шире N% (геометрия post-crash, 2R недостижим) | 10 |
| **max candidates** | сколько имён анализируется (живой режим); пусто = вся S&P 500 | пусто |

### Как считается score (5 факторов слабости)

Composite — взвешенная сумма пяти компонентов (зеркало long-VCP):

| Фактор | Вес | Что измеряет |
|---|---|---|
| **Тренд (Stage 4)** | 30% | ниже MA200 (40) + death cross MA50<MA200 (25) + ниже MA50 (20) + MA50 падает (15) |
| **Отн. слабость** | 25% | отставание от индекса за RS lookback (−20% и хуже → 100) |
| **Пробой базы** | 20% | пробит саппорт (50) + расширение объёма на пробое (до 50) |
| **Нижние максимумы** | 15% | свежий 20-дн. максимум ниже предыдущего (−10% и больше → 100) |
| **Ликвидность / borrow** | 10% | оборот ≥ $50M → 100, ≥ $10M → 60, ≥ $3M → 30; цена < $5 ½ балла |

### Грейды

- **A (80–100)** — чистая слабость Stage 4, приоритетный кандидат.
- **B (65–79)** — сильная слабость, торгуемо на подтверждённом пробое.
- **C (50–64)** — развивающаяся слабость, watchlist (ждать чище пробой).
- **D (< 50)** — слабый сигнал, пропустить.

**Кэп по состоянию.** Если бумага *перепродана / растянута* вниз (RSI < 25 или цена
ниже MA50 более чем на 20%), грейд срезается до **C**: шортить «падающий нож»
поздно — высок риск отскока к среднему. В разборе строки видно соответствующее
предупреждение.

### Уровни сделки

- **Вход** — около текущей цены.
- **Стоп** — НАД последним нижним максимумом + буфер 0.5×ATR (вне однобарного шума).
- **Цель (2R)** — на расстоянии 2× риска ниже входа.

Кандидаты с непригодной геометрией стопа (тоньше \`min стоп %\` или шире \`max стоп %\`)
отсеиваются ещё до отчёта.

### Требования

Скрин читает живые данные TradingView — нужен запущенный **TradingView Desktop**
(CDP :9222). Задачи идут через общий однопоточный лок: пока выполняется одна,
остальные кнопки (включая VCP-скрин и слоты планировщика) вернут «занято».
`;

export default function ShortScreenerHelpModal({ onClose }: { onClose: () => void }) {
  return (
    <Modal
      title="❓ Документация — скринер шортов"
      onClose={onClose}
      wide
      footer={<button onClick={onClose}>Закрыть</button>}
    >
      <div className="md">
        <Markdown remarkPlugins={[remarkGfm]}>{HELP}</Markdown>
      </div>
    </Modal>
  );
}
