import Markdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import { Modal } from './ui';

const HELP = `
## Как работать со скринером

Вкладка повторяет вечерний прогон планировщика (шаги **5.1 → 5.3 → 5.4**), но
под вашим контролем и **ничего не записывает**, пока вы не нажмёте «Сохранить».
Промежуточные файлы лежат в \`trading-data/ui-staging/\` и не видны остальному
дашборду до сохранения.

### Порядок действий

1. **▶ Запустить скрин (5.1).** Прогоняет \`vcp-screener\` с вашими параметрами по
   живым данным TradingView. Результат — топ-100 кандидатов по composite score.
2. **🧮 Построить план (5.4).** По staged-результату гоняет \`breakout-trade-planner\`:
   вход/стоп/цель/количество, earnings-гейт, heat-лимит. Кандидаты делятся на
   *в сделку / ревалидация / отклонено / earnings-блок / heat-defer*.
3. **💾 Сохранить.** Только теперь данные регистрируются (см. ниже).

### Параметры скрина

Поля предзаполнены значениями, с которыми скринер запускается в **evening-prep**
(это дефолты самого скрипта). Пустое поле = дефолт скрипта.

| Параметр | Что значит | Дефолт |
|---|---|---|
| **Вселенная** | S&P 500 / широкая NASDAQ+NYSE (файл) / свой список тикеров | wide, если есть файл |
| **trend-min-score** | минимальный сырой балл Trend Template (фильтр фазы 2) | 85 |
| **min-atr %** | мин. средний дневной диапазон — отсекает «мёртвые» бумаги | 1.0 |
| **breakout-vol ×** | объём пробоя ≥ N× от среднего за 50 дней | 1.5 |
| **min-сжатий** | минимум контракций для валидного VCP (2–4) | 2 |
| **ext-threshold** | дистанция от SMA50, с которой начинается штраф за «растянутость» | 8.0 |
| **max-candidates** | сколько бумаг доходит до полного VCP-анализа; пусто = вся вселенная, число = ограничить сверху | пусто (S&P 500 → 100) |
| **режим** | \`все\` или только \`pre-breakout\` (entry-ready) | все |
| **strict** | только valid VCP в состоянии Pre-breakout/Breakout | выкл |
| **earnings-гейт (дн.)** | блок плана, если отчёт ближе N торговых дней (шаг 5.4) | 10 |

### Как считается score

Composite — взвешенная сумма пяти компонентов: **Trend Template 25%**, **Сжатие
(VCP) 25%**, **Объём (dry-up) 20%**, **Близость к pivot 15%**, **Relative Strength
15%**. Разверните строку кандидата (▸), чтобы увидеть вклад каждого компонента,
7 критериев Trend Template, таблицу сжатий, dry-up объёма, риск до стопа и RS по
периодам.

### Чек-лист 5.3 («беру / не беру»)

Семь условий, которые должны совпасть, чтобы сделка имела смысл. В колонке
**5.3** видно, сколько пунктов уже выполнено (например \`5/7\`). Значки:
**✓** — выполнен, **✗** — нарушен (бумагу лучше пропустить), **?** — пока нельзя
проверить. Пункты 1–5 считаются сразу из скрина; пункты 6 и 7 появляются после
кнопки «Построить план».

Каждый пункт простыми словами:

1. **Гейт = allow.** «Светофор» рынка на сегодня (его считает вечерний анализ
   широты рынка). \`allow\` — рынок здоровый, открывать лонги можно; \`restrict\` /
   \`cash-priority\` — рынок слабый, новые покупки нежелательны. Если светофора за
   сегодня нет — стоит **?**.
2. **Цена > SMA50 > SMA200.** SMA — это «скользящая средняя», средняя цена за
   последние N дней (50 и 200). Когда текущая цена выше средней за 50 дней, а та
   выше средней за 200, бумага в устойчивом восходящем тренде (стадия роста по
   Минервини). Если порядок нарушен — тренд ещё не подтверждён.
3. **RS-перцентиль ≥ 70.** RS (relative strength) — насколько бумага сильнее
   рынка. Перцентиль 70 значит «сильнее 70% всех акций». Минервини берёт только
   лидеров: чем выше — тем лучше.
4. **Цена ≥ $15, оборот ≥ $25M/день.** Фильтр ликвидности: дешёвые (< $15) и
   малоторгуемые бумаги двигаются рвано, по ним большой спред и проскальзывание.
   Оборот = цена × средний объём за 50 дней. Должно быть не меньше $25 млн в день.
5. **Чёткая база, стоп ≤ 8% от входа.** «База» — период затишья (сжатия) перед
   пробоем, из которого понятны точка входа и стоп-лосс. Расстояние от входа до
   стопа должно укладываться в 8% — иначе риск на сделку слишком большой. Если
   паттерн невалидный (\`valid VCP = нет\`) — пункт сразу ✗.
6. **До отчёта > 10 торговых дней.** В день квартального отчёта (earnings) цена
   часто прыгает на разрыве, и стоп не спасает. Поэтому если отчёт ближе 10
   торговых дней — вход блокируется. Считается на шаге «Построить план» (нужен
   earnings-гейт), до этого — **?**.
7. **После сделки: heat ≤ 6%, позиций ≤ 6.** «Heat» — суммарный риск всех
   открытых позиций (сколько потеряешь, если все выбьет по стопам), в процентах от
   счёта. Лимиты: суммарный риск с этой сделкой ≤ 6% и не больше 6 позиций
   одновременно. Считается из журнала heat после «Построить план», до этого — **?**.

Идеальный кандидат — **7/7**. Пункты с **?** не означают провал: просто постройте
план, и недостающие подтянутся.

### Кнопки сохранения

- **💾 Сохранить как watchlist** — пишет \`watchlist_<сегодня>.json\` (с реальным
  гейтом дня) и копирует staged screener/plan в \`screeners/\` и \`plans/\`.
- **💾 + журнал + алерты (5.5)** — то же плюс регистрация тезисов
  (trader-memory-core) и алерты TradingView \`[WL]\` (нужен TradingView Desktop).

### Требования

Скрин и план читают живые данные TradingView — нужен запущенный **TradingView
Desktop** (CDP :9222). Задачи идут через общий однопоточный лок: пока выполняется
одна, остальные кнопки (включая слоты планировщика) вернут «занято».
`;

export default function ScreenerHelpModal({ onClose }: { onClose: () => void }) {
  return (
    <Modal
      title="❓ Документация — скринер"
      onClose={onClose}
      wide
      footer={<button onClick={onClose}>Закрыть</button>}
    >
      <div className="md">
        <Markdown remarkPlugins={[remarkGfm]}>{HELP}</Markdown>
      </div>
    </Modal>
  );
}
