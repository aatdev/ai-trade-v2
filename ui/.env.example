# Web UI configuration (all optional — sane defaults are derived automatically).
# Copy to `ui/.env` (gitignored) and edit. Loaded at server startup.

# Port the Express server listens on (prod single-port mode and dev API).
PORT=4000

# --- Authentication (optional) -----------------------------------------------
# Set BOTH to require a login (username + password) before the dashboard loads.
# Leave either blank/unset to disable auth entirely (original behavior).
# The session is a signed, httpOnly cookie; no credentials are stored client-side.
# UI_AUTH_USER=trader
# UI_AUTH_PASSWORD=change-me

# Optional: explicit signing key for session cookies. If unset, the key is
# derived from the credentials (so changing the password logs everyone out).
# Set a stable value to keep sessions valid across password changes.
# UI_AUTH_SECRET=some-long-random-string

# Optional: session lifetime in hours (default 168 = 7 days).
# UI_AUTH_TTL_HOURS=168

# Override the trading-data directory. Mirrors the scheduler's TRADING_DATE_DIR
# env var (note: DATE, not DATA). Absolute path, or relative to the repo root.
# TRADING_DATE_DIR=trading-data

# Override the repository root. By default the server walks up from its own
# location / cwd until it finds scripts/run_trading_schedule.py.
# TRADING_PROJECT_ROOT=/absolute/path/to/claude-trading-skills

# Model used for "Analyze ticker" headless runs (default: claude-opus-4-8).
# TRADING_UI_ANALYZE_MODEL=claude-opus-4-8

# --mcp-config file for "Analyze ticker". By default the server generates one
# that registers the vendored TradingView MCP server (vendor/tradingview-mcp).
# Set this to use a different MCP config (e.g. another checkout).
# Requires TradingView Desktop running with CDP on :9222 (./run_tw.sh).
# TRADING_UI_MCP_CONFIG=/absolute/path/to/custom.mcp.json
